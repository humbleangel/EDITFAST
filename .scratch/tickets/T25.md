## Parent

Part of #1

## What to build

Complete README that documents install, usage, env vars, troubleshooting, and the opencode-bug workaround that motivates this tool.

File: `README.md` (overwrite the initial stub)

Sections:
1. **What** — one paragraph: custom-tool reproduction of `opencode-fast-apply`, bypassing opencode bugs #33455 and #31354
2. **Why** — link to `docs/research/custom-tools-pathway.md` and explain the workaround briefly
3. **Install** — `bun run install` (or manual copy), plus the requirement that opencode's `tools/` directory is auto-scanned
4. **Configure** — env var table:

| Var | Default | Description |
|-----|---------|-------------|
| `FAST_APPLY_URL` | `http://localhost:1234/v1` | OpenAI-compatible API base (without trailing `/v1`) |
| `FAST_APPLY_MODEL` | `fastapply-1.5b` | Model name |
| `FAST_APPLY_API_KEY` | `optional-api-key` | Bearer token |

5. **Usage** — example call to `fast_apply_edit`, with arg descriptions (taken from `TOOL_INSTRUCTIONS`)
6. **Troubleshooting** — common failures with their messages and the recommended action (e.g. "Cannot locate original_code" → re-read the file; API failures → fall back to native `edit` tool)
7. **Development** — `bun install`, `bun test`, `bun run typecheck`, `bun run install`
8. **Limitations** — no TUI notifications (custom-tool API doesn't provide `client`); same limitations as the upstream plugin
9. **Credits** — credit `tickernelz/opencode-fast-apply` as the source of the design

## Acceptance criteria

- [ ] All 9 sections present
- [ ] Env var table is accurate (matches `src/config.ts`)
- [ ] Install command works as documented
- [ ] Limitations section explicitly mentions TUI notifications gap
- [ ] Credits `tickernelz/opencode-fast-apply`

## Blocked by

None — can start immediately.
