## Parent

Part of #1

## What to build

The TOOL_INSTRUCTIONS constant — the long-form description string the LLM sees when deciding whether to call the tool. Identical to the original plugin's text.

File: `src/instructions.ts`

Exports:
- `TOOL_INSTRUCTIONS: string` — verbatim copy of the `TOOL_INSTRUCTIONS` constant from `tickernelz/opencode-fast-apply/index.ts`

The string instructs the LLM that:
- This is the DEFAULT tool for editing existing files (use INSTEAD of native `edit`)
- For EXISTING files only — use `write` for new files
- Workflow: read file → extract 50–500 lines context → call `fast_apply_edit` with `original_code` (partial) and `code_edit`
- Partial editing: don't send entire file; use `// ... existing code ...` markers
- Priority: fast_apply_edit (default) > edit (fallback if API fails) > write (new files only)
- Format rules, examples, fallback

Reference: `tickernelz/opencode-fast-apply` `TOOL_INSTRUCTIONS` (full multi-line string).

No unit tests for this ticket — verified by snapshot/integration in T20.

## Acceptance criteria

- [ ] `TOOL_INSTRUCTIONS` exported from `src/instructions.ts`
- [ ] String matches the reference plugin's text byte-for-byte
- [ ] Contains the key phrases: "DEFAULT tool for editing existing files", "Use INSTEAD of native 'edit' tool", "fast_apply_edit", "// ... existing code ..."
- [ ] `bun run typecheck` passes

## Blocked by

None — can start immediately.
