## Parent

Part of #1

## What to build

Initialise the EDITFAST project structure so any other ticket can run.

- `package.json` with dependencies: `@opencode-ai/plugin` (^1.17.11), `diff` (^7.0.0), `zod` (^4.1.8); devDeps: `typescript`, `@types/node`, `bun-types`, `vitest`
- `tsconfig.json` with strict mode, ESNext target, moduleResolution: "bundler", allowImportingTsExtensions: true
- Source layout: `src/` for implementation files, `tests/` for test files
- `bun test` exits 0 with zero tests
- `bun run typecheck` (alias for `tsc --noEmit`) exits 0

## Acceptance criteria

- [ ] `bun test` exits 0 with no test files
- [ ] `bun run typecheck` exits 0
- [ ] `package.json` lists all required deps and devDeps
- [ ] `tsconfig.json` allows importing `@opencode-ai/plugin` types
- [ ] `src/` and `tests/` directories exist

## Blocked by

None — can start immediately.
