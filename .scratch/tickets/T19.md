## Parent

Part of #1

## What to build

The OpenCode custom tool registration shell — `tool()` export with the full description (from T18) and arg schema (using Zod). The `execute` body is a placeholder that returns `"not implemented"` so this ticket can be wired in isolation.

File: `src/tool.ts`

Exports:
- `default export` — the registered tool

Logic:
```
import { tool } from "@opencode-ai/plugin"
import { TOOL_INSTRUCTIONS } from "./instructions"

export default tool({
  description: TOOL_INSTRUCTIONS,
  args: {
    target_filepath: tool.schema
      .string()
      .describe("Path of the file to modify (relative to project root)"),
    original_code: tool.schema
      .string()
      .describe("The original code section to be modified. Provide 50-500 lines of context..."),
    code_edit: tool.schema
      .string()
      .describe("The updated code with changes applied. Use '// ... existing code ...' markers..."),
  },
  async execute(args, context) {
    return "T19 stub: tool shell wired, execute pending (T20)"
  }
})
```

The arg descriptions should follow the original plugin's wording closely — they guide the LLM on what to send.

No unit test for this ticket — covered by T22 (integration test).

## Acceptance criteria

- [ ] `src/tool.ts` exports a default `tool({...})` object
- [ ] Description matches `TOOL_INSTRUCTIONS` from T18
- [ ] Args has `target_filepath`, `original_code`, `code_edit`, all Zod strings with descriptive strings
- [ ] `bun run typecheck` passes
- [ ] File is importable (sanity: `bun -e 'import("./src/tool.ts")'`)

## Blocked by

None — can start immediately.
